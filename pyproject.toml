[project]
name = "ria"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "scikit-learn",
    "pyyaml",
    "pandas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ria"]
