[project]
name = "ria"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "json",
    "scikit-learn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ria"]
